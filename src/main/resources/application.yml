spring:
  profiles:
    group:
      local:
        - local-mysql
        - local-token
      dev:
        - dev-mysql
        - dev-token
    active: dev
logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    org.springframework.web: DEBUG