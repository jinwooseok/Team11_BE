spring:
  config:
    activate:
      on-profile: dev-token

security:
  jwt:
    token:
      secretKey: ${JWT_SECRET_KEY}
      access:
        validTime: 15000
      refresh:
        validTime: 30000
